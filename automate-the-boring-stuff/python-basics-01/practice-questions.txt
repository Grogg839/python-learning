1. * is an operator
'hello' is a value 
-88.8 is a value 
- is an operator 
/ is an operator 
+ is an operator 
5 is a value 

2. spam is a variable and 'spam' is a string. 

3. integar, floating point number and string are 3 data types. 

4. an expression is made of 2 or more values and one operator, and all expressions evaluate to a value of some kind.

5. an expression is a way of showing something, i.e 5x5. a statement is a way of showing something that is TRUE, i.e 5x5=25.

6. bacon = 20
   bacon + 1 would evaluate to 21.

7. 'spam' + 'spamspam'
   'spam' * 3 would evalue to the word spam 9 time next to eachother. 

8. eggs is a fine variable name as it only uses latters, 100 is not a fine variable name as it starts with a number which is not allowed.

9. int(), float() and str() are functions used to get the integar, float or string versions of a value.

10. 'I have eaten ' + 99 + ' burritos. ' causes an error as 99 is just a value, and can't be a variable name or anything as it starts with a number.
One way of fixing it would be to just make the whole thing a string in quotation marks ''.
